export const <%= camelize(plural) %>BodyReadMultipleSchema = {
    type: 'object',
    properties: {
        <%= camelize(name) %>IDs: {
            type: 'array',
            items: {
                type: 'string',
            },
        },
    },
};
