export const <%= camelize(plural) %>BodyDeleteMultipleSchema = {
    type: 'object',
    properties: {
        <%= camelize(plural) %>IDs: {
            type: 'array',
            items: {
                type: 'string',
            },
        },
    },
};
