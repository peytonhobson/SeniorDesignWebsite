import * as schemas from './schemas';

export const <%= camelize(name) %>Action = {
    body: schemas.<%= camelize(name) %>ActionBodySchema,
    response: {
        200: schemas.<%= camelize(name) %>ActionResponseObjectSchema,
    },
};
