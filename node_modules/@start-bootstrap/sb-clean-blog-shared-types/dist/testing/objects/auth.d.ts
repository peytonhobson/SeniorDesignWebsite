import { LoginPayload, TokenResponse, DecodedToken, UserForToken } from '../../typings/auth';
export declare class TestLoginPayload implements LoginPayload {
    password: string;
}
export declare class TestLoginResults implements TokenResponse {
    token: string;
}
export declare class TestDecodedToken implements DecodedToken {
    exp: number;
    iat: number;
    id: string;
    firstName: string;
    lastName: string;
    email: string;
}
export declare class TestUserForToken implements UserForToken {
    id: string;
    firstName: string;
    lastName: string;
    email: string;
}
